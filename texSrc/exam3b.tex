\input{headerExams}

\ExamClass{Logical computational thinking}
\ExamName{Exam 3 B}
\ExamHead{25 November 2015}

\begin{document}
\ExamNameLine

\ExamDiagramBoxEn
All the variables are natural.
\begin{center}
  \begin{tikzpicture}[node distance=5mm, auto]
    \node(start) [fStartEnd] {Start};
    \node(inputN) [fInput, below=of start] {m};
    \draw [arrow] (start) -- (inputN);
    \node(iteration) [fSelection, below=7mm of inputN] {m>0};
    \draw [arrow] (inputN) -- (iteration);
    \node(selection) [fSelection, below= of iteration] {m\%2==0};
    \draw [arrow] (iteration) -- node [near start] {true} (selection);
    \node(assignation) [fProcess, below=of selection] {k=2*m};
    \draw [arrow] (selection) -- node [near start] {true} (assignation);
    \node(assignation2) [fProcess, right=5mm of assignation] {k=2*(m+1)};
    \draw [arrow] (selection) -| node [near start] {false} (assignation2);
    \node(output) [fOutput, below=7mm of assignation] {k};
    \draw [arrow] (assignation) -- (output);
    \draw [arrow] (assignation2) |- ($ (output.north) + (0,2.5mm) $);
    \node(increment) [fProcess, below=of output] {m=m-1};
    \draw [arrow] (output) -- (increment);
    \node(end) [fStartEnd, below=20mm of increment] {End};
    \draw [arrow] (increment)
    -- ($ (increment.south) - (0,5mm) $)
    -| ($ (iteration.west) - (10mm,0) $)
    |- ($ (iteration.north) + (0,2.5mm) $);
    \draw [arrow] (iteration)
    -- node [near start] {false} ($ (iteration.east) + (30mm,0) $)
    |- ($ (end.north) + (0,10mm) $)
    -- (end);
  \end{tikzpicture}
\end{center}  
\newpage
\ExamCompleteBoxEn
Write a program that read a natural number ``$i$'' and calculate and
write the sum:
$$
2+2^2+2^3+\dots+2^i
$$
\end{document}
